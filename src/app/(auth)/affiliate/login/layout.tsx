import { ReactNode } from "react"

export const metadata = {
  title: 'Affiliate Login | The Grind Academy',
  description: 'Login to your The Grind Academy affiliate account'
}

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
