import { Fragment, ReactNode } from "react"

export const metadata = {
  title: 'Support | The Grind Academy'
}

export default function SupportLayout({ children }: { children: ReactNode }) {
  return <Fragment>{children}</Fragment>
}
