export enum RoleEnum {
}

export enum UserStatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended'
}

export enum PaymentStatusEnum {
  PENDING = 'pending',
  PAID = 'paid',
  FAILED = 'failed',
  REFUNDED = 'refunded',
}

export enum URLKeyEnum {
  LOGOUT = 'logout',
  REDIRECT = 'redirect',
  BACK_TEXT = 'backText',
  BACK_URL = 'backUrl',
}

export enum TimeFrameEnum {
  SEVEN_DAYS = '7-days',
  THIRTY_DAYS = '30-days',
  TWELVE_MONTHS = '12-months',
}