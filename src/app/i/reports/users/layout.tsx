import { Fragment, ReactNode } from "react"

export const metadata = {
  title: 'User Reports | The Grind Academy'
}

export default function UsersReportsLayout({ children }: { children: ReactNode }) {
  return <Fragment>{children}</Fragment>
}