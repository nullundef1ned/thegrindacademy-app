import { Fragment, ReactNode } from "react"

export const metadata = {
  title: 'Subscription Reports | The Grind Academy'
}

export default function SubscriptionReportsLayout({ children }: { children: ReactNode }) {
  return <Fragment>{children}</Fragment>
}