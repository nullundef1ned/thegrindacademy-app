import { Fragment, ReactNode } from "react"

export const metadata = {
  title: `FAQ | The Grind Academy`
}

export default function FAQsDetailLayout({ children }: { children: ReactNode }) {
  return <Fragment>{children}</Fragment>
}