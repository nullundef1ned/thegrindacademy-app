import { Fragment, ReactNode } from "react"

export const metadata = {
  title: 'Privacy Policy | The Grind Academy'
}

export default function PrivacyPolicyLayout({ children }: { children: ReactNode }) {
  return <Fragment>{children}</Fragment>
}
